import {
  fanIn,
  fanOut,
  fanOut_fanIn,
  group,
  sequence,
  optional,
  repeat,
  choice,
  merge,
  branch,
  terminal
} from "@imaguiraga/topology-dsl-core";

/**
 * Create a az_Notification_Hub_Namespaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hub_Namespaces(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hub_Namespaces");
}

/**
 * Create a az_Azure_Media_Service dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Media_Service(...elts) {
  return terminal(...elts)._subType_("az_Azure_Media_Service");
}

/**
 * Create a az_Recovery_Services_Vaults dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Recovery_Services_Vaults(...elts) {
  return terminal(...elts)._subType_("az_Recovery_Services_Vaults");
}

/**
 * Create a az_Data_Lake_Store_Gen1 dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Lake_Store_Gen1(...elts) {
  return terminal(...elts)._subType_("az_Data_Lake_Store_Gen1");
}

/**
 * Create a az_Azure_Stack_Edge dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Stack_Edge(...elts) {
  return terminal(...elts)._subType_("az_Azure_Stack_Edge");
}

/**
 * Create a az_Azure_HCP_Cache dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_HCP_Cache(...elts) {
  return terminal(...elts)._subType_("az_Azure_HCP_Cache");
}

/**
 * Create a az_Storage_Accounts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Storage_Accounts(...elts) {
  return terminal(...elts)._subType_("az_Storage_Accounts");
}

/**
 * Create a az_Storage_Accounts__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Storage_Accounts__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Storage_Accounts__Classic_");
}

/**
 * Create a az_StorSimple_Device_Managers dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_StorSimple_Device_Managers(...elts) {
  return terminal(...elts)._subType_("az_StorSimple_Device_Managers");
}

/**
 * Create a az_Data_Lake_Storage_Gen1 dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Lake_Storage_Gen1(...elts) {
  return terminal(...elts)._subType_("az_Data_Lake_Storage_Gen1");
}

/**
 * Create a az_StorSimple_Data_Managers dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_StorSimple_Data_Managers(...elts) {
  return terminal(...elts)._subType_("az_StorSimple_Data_Managers");
}

/**
 * Create a az_Data_Box dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Box(...elts) {
  return terminal(...elts)._subType_("az_Data_Box");
}

/**
 * Create a az_Data_Box_Edge dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Box_Edge(...elts) {
  return terminal(...elts)._subType_("az_Data_Box_Edge");
}

/**
 * Create a az_Azure_NetApp_Files dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_NetApp_Files(...elts) {
  return terminal(...elts)._subType_("az_Azure_NetApp_Files");
}

/**
 * Create a az_Data_Share_Invitations dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Share_Invitations(...elts) {
  return terminal(...elts)._subType_("az_Data_Share_Invitations");
}

/**
 * Create a az_Data_Shares dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Shares(...elts) {
  return terminal(...elts)._subType_("az_Data_Shares");
}

/**
 * Create a az_Import_Export_Jobs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Import_Export_Jobs(...elts) {
  return terminal(...elts)._subType_("az_Import_Export_Jobs");
}

/**
 * Create a az_Azure_Cloud_Shell dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Cloud_Shell(...elts) {
  return terminal(...elts)._subType_("az_Azure_Cloud_Shell");
}

/**
 * Create a az_Private_Link_Hub dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Private_Link_Hub(...elts) {
  return terminal(...elts)._subType_("az_Private_Link_Hub");
}

/**
 * Create a az_Web_Environment dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Web_Environment(...elts) {
  return terminal(...elts)._subType_("az_Web_Environment");
}

/**
 * Create a az_Detonation dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Detonation(...elts) {
  return terminal(...elts)._subType_("az_Detonation");
}

/**
 * Create a az_Disk_Encryption_Sets dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Disk_Encryption_Sets(...elts) {
  return terminal(...elts)._subType_("az_Disk_Encryption_Sets");
}

/**
 * Create a az_Internet_Analyzer_Profiles dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Internet_Analyzer_Profiles(...elts) {
  return terminal(...elts)._subType_("az_Internet_Analyzer_Profiles");
}

/**
 * Create a az_IP_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_IP_Groups(...elts) {
  return terminal(...elts)._subType_("az_IP_Groups");
}

/**
 * Create a az_Reservations dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Reservations(...elts) {
  return terminal(...elts)._subType_("az_Reservations");
}

/**
 * Create a az_Application_Gateways dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Application_Gateways(...elts) {
  return terminal(...elts)._subType_("az_Application_Gateways");
}

/**
 * Create a az_Firewalls dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Firewalls(...elts) {
  return terminal(...elts)._subType_("az_Firewalls");
}

/**
 * Create a az_Instance_Pools dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Instance_Pools(...elts) {
  return terminal(...elts)._subType_("az_Instance_Pools");
}

/**
 * Create a az_NAT dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_NAT(...elts) {
  return terminal(...elts)._subType_("az_NAT");
}

/**
 * Create a az_Network_Watcher dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Network_Watcher(...elts) {
  return terminal(...elts)._subType_("az_Network_Watcher");
}

/**
 * Create a az_CDN_Profiles dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_CDN_Profiles(...elts) {
  return terminal(...elts)._subType_("az_CDN_Profiles");
}

/**
 * Create a az_Azure_Firewall_Manager dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Firewall_Manager(...elts) {
  return terminal(...elts)._subType_("az_Azure_Firewall_Manager");
}

/**
 * Create a az_Private_Link dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Private_Link(...elts) {
  return terminal(...elts)._subType_("az_Private_Link");
}

/**
 * Create a az_Private_Link_Service dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Private_Link_Service(...elts) {
  return terminal(...elts)._subType_("az_Private_Link_Service");
}

/**
 * Create a az_Virtual_Networks dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Networks(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Networks");
}

/**
 * Create a az_Load_Balancers dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Load_Balancers(...elts) {
  return terminal(...elts)._subType_("az_Load_Balancers");
}

/**
 * Create a az_Virtual_Network_Gateways dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Network_Gateways(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Network_Gateways");
}

/**
 * Create a az_DNS_Zones dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_DNS_Zones(...elts) {
  return terminal(...elts)._subType_("az_DNS_Zones");
}

/**
 * Create a az_Traffic_Manager_Profiles dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Traffic_Manager_Profiles(...elts) {
  return terminal(...elts)._subType_("az_Traffic_Manager_Profiles");
}

/**
 * Create a az_Network_Watcher dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Network_Watcher(...elts) {
  return terminal(...elts)._subType_("az_Network_Watcher");
}

/**
 * Create a az_Network_Security_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Network_Security_Groups(...elts) {
  return terminal(...elts)._subType_("az_Network_Security_Groups");
}

/**
 * Create a az_Public_IP_Addresses__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Public_IP_Addresses__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Public_IP_Addresses__Classic_");
}

/**
 * Create a az_Public_IP_Addresses dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Public_IP_Addresses(...elts) {
  return terminal(...elts)._subType_("az_Public_IP_Addresses");
}

/**
 * Create a az_Route_Filters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Route_Filters(...elts) {
  return terminal(...elts)._subType_("az_Route_Filters");
}

/**
 * Create a az_DDoS_Protection_Plans dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_DDoS_Protection_Plans(...elts) {
  return terminal(...elts)._subType_("az_DDoS_Protection_Plans");
}

/**
 * Create a az_Front_Doors dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Front_Doors(...elts) {
  return terminal(...elts)._subType_("az_Front_Doors");
}

/**
 * Create a az_Virtual_Networks__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Networks__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Networks__Classic_");
}

/**
 * Create a az_ExpressRoute_Circuits dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_ExpressRoute_Circuits(...elts) {
  return terminal(...elts)._subType_("az_ExpressRoute_Circuits");
}

/**
 * Create a az_Network_Interfaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Network_Interfaces(...elts) {
  return terminal(...elts)._subType_("az_Network_Interfaces");
}

/**
 * Create a az_Connections dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Connections(...elts) {
  return terminal(...elts)._subType_("az_Connections");
}

/**
 * Create a az_Application_Security_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Application_Security_Groups(...elts) {
  return terminal(...elts)._subType_("az_Application_Security_Groups");
}

/**
 * Create a az_Service_Endpoint_Policies dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Endpoint_Policies(...elts) {
  return terminal(...elts)._subType_("az_Service_Endpoint_Policies");
}

/**
 * Create a az_NAT dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_NAT(...elts) {
  return terminal(...elts)._subType_("az_NAT");
}

/**
 * Create a az_Web_Application_Firewall_Policies_WAF_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Web_Application_Firewall_Policies_WAF_(...elts) {
  return terminal(...elts)._subType_("az_Web_Application_Firewall_Policies_WAF_");
}

/**
 * Create a az_Proximity_Placement_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Proximity_Placement_Groups(...elts) {
  return terminal(...elts)._subType_("az_Proximity_Placement_Groups");
}

/**
 * Create a az_Reserved_IP_Addresses__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Reserved_IP_Addresses__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Reserved_IP_Addresses__Classic_");
}

/**
 * Create a az_Public_IP_Prefixes dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Public_IP_Prefixes(...elts) {
  return terminal(...elts)._subType_("az_Public_IP_Prefixes");
}

/**
 * Create a az_Remote_Rendering dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Remote_Rendering(...elts) {
  return terminal(...elts)._subType_("az_Remote_Rendering");
}

/**
 * Create a az_Recovery_Services_Vaults dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Recovery_Services_Vaults(...elts) {
  return terminal(...elts)._subType_("az_Recovery_Services_Vaults");
}

/**
 * Create a az_Data_Box dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Box(...elts) {
  return terminal(...elts)._subType_("az_Data_Box");
}

/**
 * Create a az_Data_Box_Edge dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Box_Edge(...elts) {
  return terminal(...elts)._subType_("az_Data_Box_Edge");
}

/**
 * Create a az_Monitor dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Monitor(...elts) {
  return terminal(...elts)._subType_("az_Monitor");
}

/**
 * Create a az_Alerts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Alerts(...elts) {
  return terminal(...elts)._subType_("az_Alerts");
}

/**
 * Create a az_Advisor dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Advisor(...elts) {
  return terminal(...elts)._subType_("az_Advisor");
}

/**
 * Create a az_User_Privacy dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_User_Privacy(...elts) {
  return terminal(...elts)._subType_("az_User_Privacy");
}

/**
 * Create a az_Blueprints dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Blueprints(...elts) {
  return terminal(...elts)._subType_("az_Blueprints");
}

/**
 * Create a az_Activity_Log dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Activity_Log(...elts) {
  return terminal(...elts)._subType_("az_Activity_Log");
}

/**
 * Create a az_Diagnostics_Settings dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Diagnostics_Settings(...elts) {
  return terminal(...elts)._subType_("az_Diagnostics_Settings");
}

/**
 * Create a az_Log_Analytics_Workspaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Log_Analytics_Workspaces(...elts) {
  return terminal(...elts)._subType_("az_Log_Analytics_Workspaces");
}

/**
 * Create a az_Application_Insights dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Application_Insights(...elts) {
  return terminal(...elts)._subType_("az_Application_Insights");
}

/**
 * Create a az_My_Customers dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_My_Customers(...elts) {
  return terminal(...elts)._subType_("az_My_Customers");
}

/**
 * Create a az_Managed_Applications_Center dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Managed_Applications_Center(...elts) {
  return terminal(...elts)._subType_("az_Managed_Applications_Center");
}

/**
 * Create a az_Recovery_Services_Vaults dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Recovery_Services_Vaults(...elts) {
  return terminal(...elts)._subType_("az_Recovery_Services_Vaults");
}

/**
 * Create a az_Policy dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Policy(...elts) {
  return terminal(...elts)._subType_("az_Policy");
}

/**
 * Create a az_Resource_Graph_Explorer dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Resource_Graph_Explorer(...elts) {
  return terminal(...elts)._subType_("az_Resource_Graph_Explorer");
}

/**
 * Create a az_Metrics dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Metrics(...elts) {
  return terminal(...elts)._subType_("az_Metrics");
}

/**
 * Create a az_Solutions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Solutions(...elts) {
  return terminal(...elts)._subType_("az_Solutions");
}

/**
 * Create a az_Automation_Accounts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Automation_Accounts(...elts) {
  return terminal(...elts)._subType_("az_Automation_Accounts");
}

/**
 * Create a az_Network_Watcher dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Network_Watcher(...elts) {
  return terminal(...elts)._subType_("az_Network_Watcher");
}

/**
 * Create a az_Operation_Log__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Operation_Log__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Operation_Log__Classic_");
}

/**
 * Create a az_Service_Providers dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Providers(...elts) {
  return terminal(...elts)._subType_("az_Service_Providers");
}

/**
 * Create a az_Education dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Education(...elts) {
  return terminal(...elts)._subType_("az_Education");
}

/**
 * Create a az_Azure_Lighthouse dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Lighthouse(...elts) {
  return terminal(...elts)._subType_("az_Azure_Lighthouse");
}

/**
 * Create a az_Azure_Arc dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Arc(...elts) {
  return terminal(...elts)._subType_("az_Azure_Arc");
}

/**
 * Create a az_User_Privacy dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_User_Privacy(...elts) {
  return terminal(...elts)._subType_("az_User_Privacy");
}

/**
 * Create a az_Managed_Applications_Center dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Managed_Applications_Center(...elts) {
  return terminal(...elts)._subType_("az_Managed_Applications_Center");
}

/**
 * Create a az_Policy dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Policy(...elts) {
  return terminal(...elts)._subType_("az_Policy");
}

/**
 * Create a az_MachinesAzureArc dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_MachinesAzureArc(...elts) {
  return terminal(...elts)._subType_("az_MachinesAzureArc");
}

/**
 * Create a az_Event_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Event_Hubs");
}

/**
 * Create a az_Stream_Analytics_Jobs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Stream_Analytics_Jobs(...elts) {
  return terminal(...elts)._subType_("az_Stream_Analytics_Jobs");
}

/**
 * Create a az_Notification_Hub_Namespaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hub_Namespaces(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hub_Namespaces");
}

/**
 * Create a az_Notification_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hubs");
}

/**
 * Create a az_Function_Apps dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Function_Apps(...elts) {
  return terminal(...elts)._subType_("az_Function_Apps");
}

/**
 * Create a az_Notification_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hubs");
}

/**
 * Create a az_Event_Grid_Subscriptions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Grid_Subscriptions(...elts) {
  return terminal(...elts)._subType_("az_Event_Grid_Subscriptions");
}

/**
 * Create a az_IoT_Central_Applications dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_IoT_Central_Applications(...elts) {
  return terminal(...elts)._subType_("az_IoT_Central_Applications");
}

/**
 * Create a az_Logic_Apps dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Logic_Apps(...elts) {
  return terminal(...elts)._subType_("az_Logic_Apps");
}

/**
 * Create a az_Device_Security_Apple dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Device_Security_Apple(...elts) {
  return terminal(...elts)._subType_("az_Device_Security_Apple");
}

/**
 * Create a az_Device_Security_Google dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Device_Security_Google(...elts) {
  return terminal(...elts)._subType_("az_Device_Security_Google");
}

/**
 * Create a az_Device_Security_Windows dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Device_Security_Windows(...elts) {
  return terminal(...elts)._subType_("az_Device_Security_Windows");
}

/**
 * Create a az_SQL_Data_Warehouses dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Data_Warehouses(...elts) {
  return terminal(...elts)._subType_("az_SQL_Data_Warehouses");
}

/**
 * Create a az_Data_Factory dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Factory(...elts) {
  return terminal(...elts)._subType_("az_Data_Factory");
}

/**
 * Create a az_API_Management_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_API_Management_Services(...elts) {
  return terminal(...elts)._subType_("az_API_Management_Services");
}

/**
 * Create a az_Logic_Apps dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Logic_Apps(...elts) {
  return terminal(...elts)._subType_("az_Logic_Apps");
}

/**
 * Create a az_Event_Grid_Topics dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Grid_Topics(...elts) {
  return terminal(...elts)._subType_("az_Event_Grid_Topics");
}

/**
 * Create a az_Relays dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Relays(...elts) {
  return terminal(...elts)._subType_("az_Relays");
}

/**
 * Create a az_Service_Bus dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Bus(...elts) {
  return terminal(...elts)._subType_("az_Service_Bus");
}

/**
 * Create a az_Event_Grid_Domains dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Grid_Domains(...elts) {
  return terminal(...elts)._subType_("az_Event_Grid_Domains");
}

/**
 * Create a az_Azure_Data_Catalog dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Data_Catalog(...elts) {
  return terminal(...elts)._subType_("az_Azure_Data_Catalog");
}

/**
 * Create a az_Integration_Accounts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Integration_Accounts(...elts) {
  return terminal(...elts)._subType_("az_Integration_Accounts");
}

/**
 * Create a az_Event_Grid_Subscriptions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Grid_Subscriptions(...elts) {
  return terminal(...elts)._subType_("az_Event_Grid_Subscriptions");
}

/**
 * Create a az_Azure_Active_Directory dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Active_Directory(...elts) {
  return terminal(...elts)._subType_("az_Azure_Active_Directory");
}

/**
 * Create a az_Azure_AD_Domain_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_AD_Domain_Services(...elts) {
  return terminal(...elts)._subType_("az_Azure_AD_Domain_Services");
}

/**
 * Create a az_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Groups(...elts) {
  return terminal(...elts)._subType_("az_Groups");
}

/**
 * Create a az_Active_Directory_Connect_Health dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Active_Directory_Connect_Health(...elts) {
  return terminal(...elts)._subType_("az_Active_Directory_Connect_Health");
}

/**
 * Create a az_Enterprise_Applications dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Enterprise_Applications(...elts) {
  return terminal(...elts)._subType_("az_Enterprise_Applications");
}

/**
 * Create a az_Users dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Users(...elts) {
  return terminal(...elts)._subType_("az_Users");
}

/**
 * Create a az_App_Registrations dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Registrations(...elts) {
  return terminal(...elts)._subType_("az_App_Registrations");
}

/**
 * Create a az_Identity_Governance dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Identity_Governance(...elts) {
  return terminal(...elts)._subType_("az_Identity_Governance");
}

/**
 * Create a az_All_Resources dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_All_Resources(...elts) {
  return terminal(...elts)._subType_("az_All_Resources");
}

/**
 * Create a az_Subscriptions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Subscriptions(...elts) {
  return terminal(...elts)._subType_("az_Subscriptions");
}

/**
 * Create a az_Service_Health dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Health(...elts) {
  return terminal(...elts)._subType_("az_Service_Health");
}

/**
 * Create a az_Information dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Information(...elts) {
  return terminal(...elts)._subType_("az_Information");
}

/**
 * Create a az_Recent dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Recent(...elts) {
  return terminal(...elts)._subType_("az_Recent");
}

/**
 * Create a az_Resource_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Resource_Groups(...elts) {
  return terminal(...elts)._subType_("az_Resource_Groups");
}

/**
 * Create a az_Marketplace dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Marketplace(...elts) {
  return terminal(...elts)._subType_("az_Marketplace");
}

/**
 * Create a az_Quickstart_Center dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Quickstart_Center(...elts) {
  return terminal(...elts)._subType_("az_Quickstart_Center");
}

/**
 * Create a az_Management_Groups dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Management_Groups(...elts) {
  return terminal(...elts)._subType_("az_Management_Groups");
}

/**
 * Create a az_Help_and_Support dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Help_and_Support(...elts) {
  return terminal(...elts)._subType_("az_Help_and_Support");
}

/**
 * Create a az_Dashboard dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Dashboard(...elts) {
  return terminal(...elts)._subType_("az_Dashboard");
}

/**
 * Create a az_Cost_Management dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cost_Management(...elts) {
  return terminal(...elts)._subType_("az_Cost_Management");
}

/**
 * Create a az_Biz_Talk dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Biz_Talk(...elts) {
  return terminal(...elts)._subType_("az_Biz_Talk");
}

/**
 * Create a az_Blob_Block dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Blob_Block(...elts) {
  return terminal(...elts)._subType_("az_Blob_Block");
}

/**
 * Create a az_Blob_Page dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Blob_Page(...elts) {
  return terminal(...elts)._subType_("az_Blob_Page");
}

/**
 * Create a az_Branch dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Branch(...elts) {
  return terminal(...elts)._subType_("az_Branch");
}

/**
 * Create a az_Browser dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Browser(...elts) {
  return terminal(...elts)._subType_("az_Browser");
}

/**
 * Create a az_Bug dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Bug(...elts) {
  return terminal(...elts)._subType_("az_Bug");
}

/**
 * Create a az_Builds dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Builds(...elts) {
  return terminal(...elts)._subType_("az_Builds");
}

/**
 * Create a az_Cache dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cache(...elts) {
  return terminal(...elts)._subType_("az_Cache");
}

/**
 * Create a az_Code dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Code(...elts) {
  return terminal(...elts)._subType_("az_Code");
}

/**
 * Create a az_Commit dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Commit(...elts) {
  return terminal(...elts)._subType_("az_Commit");
}

/**
 * Create a az_Controls dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Controls(...elts) {
  return terminal(...elts)._subType_("az_Controls");
}

/**
 * Create a az_Controls_Horizontal dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Controls_Horizontal(...elts) {
  return terminal(...elts)._subType_("az_Controls_Horizontal");
}

/**
 * Create a az_Cost_Alerts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cost_Alerts(...elts) {
  return terminal(...elts)._subType_("az_Cost_Alerts");
}

/**
 * Create a az_Cost_Analysis dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cost_Analysis(...elts) {
  return terminal(...elts)._subType_("az_Cost_Analysis");
}

/**
 * Create a az_Cost_Budgets dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cost_Budgets(...elts) {
  return terminal(...elts)._subType_("az_Cost_Budgets");
}

/**
 * Create a az_Counter dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Counter(...elts) {
  return terminal(...elts)._subType_("az_Counter");
}

/**
 * Create a az_Cubes dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cubes(...elts) {
  return terminal(...elts)._subType_("az_Cubes");
}

/**
 * Create a az_Dev_Console dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Dev_Console(...elts) {
  return terminal(...elts)._subType_("az_Dev_Console");
}

/**
 * Create a az_Download dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Download(...elts) {
  return terminal(...elts)._subType_("az_Download");
}

/**
 * Create a az_Error dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Error(...elts) {
  return terminal(...elts)._subType_("az_Error");
}

/**
 * Create a az_Extensions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Extensions(...elts) {
  return terminal(...elts)._subType_("az_Extensions");
}

/**
 * Create a az_File dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_File(...elts) {
  return terminal(...elts)._subType_("az_File");
}

/**
 * Create a az_Files dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Files(...elts) {
  return terminal(...elts)._subType_("az_Files");
}

/**
 * Create a az_Folder_Blank dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Folder_Blank(...elts) {
  return terminal(...elts)._subType_("az_Folder_Blank");
}

/**
 * Create a az_Folder_Website dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Folder_Website(...elts) {
  return terminal(...elts)._subType_("az_Folder_Website");
}

/**
 * Create a az_FTP dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_FTP(...elts) {
  return terminal(...elts)._subType_("az_FTP");
}

/**
 * Create a az_Gear dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Gear(...elts) {
  return terminal(...elts)._subType_("az_Gear");
}

/**
 * Create a az_Globe dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Globe(...elts) {
  return terminal(...elts)._subType_("az_Globe");
}

/**
 * Create a az_Globe_Error dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Globe_Error(...elts) {
  return terminal(...elts)._subType_("az_Globe_Error");
}

/**
 * Create a az_Globe_Success dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Globe_Success(...elts) {
  return terminal(...elts)._subType_("az_Globe_Success");
}

/**
 * Create a az_Globe_Warning dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Globe_Warning(...elts) {
  return terminal(...elts)._subType_("az_Globe_Warning");
}

/**
 * Create a az_Guide dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Guide(...elts) {
  return terminal(...elts)._subType_("az_Guide");
}

/**
 * Create a az_Heart dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Heart(...elts) {
  return terminal(...elts)._subType_("az_Heart");
}

/**
 * Create a az_Image dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Image(...elts) {
  return terminal(...elts)._subType_("az_Image");
}

/**
 * Create a az_Input_Output dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Input_Output(...elts) {
  return terminal(...elts)._subType_("az_Input_Output");
}

/**
 * Create a az_Journey_Hub dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Journey_Hub(...elts) {
  return terminal(...elts)._subType_("az_Journey_Hub");
}

/**
 * Create a az_Launch_Portal dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Launch_Portal(...elts) {
  return terminal(...elts)._subType_("az_Launch_Portal");
}

/**
 * Create a az_Learn dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Learn(...elts) {
  return terminal(...elts)._subType_("az_Learn");
}

/**
 * Create a az_Load_Test dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Load_Test(...elts) {
  return terminal(...elts)._subType_("az_Load_Test");
}

/**
 * Create a az_Location dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Location(...elts) {
  return terminal(...elts)._subType_("az_Location");
}

/**
 * Create a az_Log_Streaming dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Log_Streaming(...elts) {
  return terminal(...elts)._subType_("az_Log_Streaming");
}

/**
 * Create a az_Management_Portal dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Management_Portal(...elts) {
  return terminal(...elts)._subType_("az_Management_Portal");
}

/**
 * Create a az_Media_File dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Media_File(...elts) {
  return terminal(...elts)._subType_("az_Media_File");
}

/**
 * Create a az_Mobile dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Mobile(...elts) {
  return terminal(...elts)._subType_("az_Mobile");
}

/**
 * Create a az_Mobile_Engagement dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Mobile_Engagement(...elts) {
  return terminal(...elts)._subType_("az_Mobile_Engagement");
}

/**
 * Create a az_Power dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Power(...elts) {
  return terminal(...elts)._subType_("az_Power");
}

/**
 * Create a az_Powershell dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Powershell(...elts) {
  return terminal(...elts)._subType_("az_Powershell");
}

/**
 * Create a az_Power_Up dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Power_Up(...elts) {
  return terminal(...elts)._subType_("az_Power_Up");
}

/**
 * Create a az_Preview dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Preview(...elts) {
  return terminal(...elts)._subType_("az_Preview");
}

/**
 * Create a az_Process_Explorer dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Process_Explorer(...elts) {
  return terminal(...elts)._subType_("az_Process_Explorer");
}

/**
 * Create a az_Production_Ready_Database dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Production_Ready_Database(...elts) {
  return terminal(...elts)._subType_("az_Production_Ready_Database");
}

/**
 * Create a az_Resource_Group_List dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Resource_Group_List(...elts) {
  return terminal(...elts)._subType_("az_Resource_Group_List");
}

/**
 * Create a az_Resource_Linked dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Resource_Linked(...elts) {
  return terminal(...elts)._subType_("az_Resource_Linked");
}

/**
 * Create a az_Scale dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Scale(...elts) {
  return terminal(...elts)._subType_("az_Scale");
}

/**
 * Create a az_Scheduler dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Scheduler(...elts) {
  return terminal(...elts)._subType_("az_Scheduler");
}

/**
 * Create a az_Search dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Search(...elts) {
  return terminal(...elts)._subType_("az_Search");
}

/**
 * Create a az_Server_Farm dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Server_Farm(...elts) {
  return terminal(...elts)._subType_("az_Server_Farm");
}

/**
 * Create a az_Service_Bus dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Bus(...elts) {
  return terminal(...elts)._subType_("az_Service_Bus");
}

/**
 * Create a az_SSD dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SSD(...elts) {
  return terminal(...elts)._subType_("az_SSD");
}

/**
 * Create a az_Storage_Azure_Files dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Storage_Azure_Files(...elts) {
  return terminal(...elts)._subType_("az_Storage_Azure_Files");
}

/**
 * Create a az_Storage_Container dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Storage_Container(...elts) {
  return terminal(...elts)._subType_("az_Storage_Container");
}

/**
 * Create a az_Storage_Queue dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Storage_Queue(...elts) {
  return terminal(...elts)._subType_("az_Storage_Queue");
}

/**
 * Create a az_Table dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Table(...elts) {
  return terminal(...elts)._subType_("az_Table");
}

/**
 * Create a az_Tags dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Tags(...elts) {
  return terminal(...elts)._subType_("az_Tags");
}

/**
 * Create a az_TFS_VC_Repository dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_TFS_VC_Repository(...elts) {
  return terminal(...elts)._subType_("az_TFS_VC_Repository");
}

/**
 * Create a az_Toolbox dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Toolbox(...elts) {
  return terminal(...elts)._subType_("az_Toolbox");
}

/**
 * Create a az_Versions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Versions(...elts) {
  return terminal(...elts)._subType_("az_Versions");
}

/**
 * Create a az_Website_Power dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Website_Power(...elts) {
  return terminal(...elts)._subType_("az_Website_Power");
}

/**
 * Create a az_Website_Staging dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Website_Staging(...elts) {
  return terminal(...elts)._subType_("az_Website_Staging");
}

/**
 * Create a az_Web_Slots dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Web_Slots(...elts) {
  return terminal(...elts)._subType_("az_Web_Slots");
}

/**
 * Create a az_Web_Test dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Web_Test(...elts) {
  return terminal(...elts)._subType_("az_Web_Test");
}

/**
 * Create a az_Workbooks dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Workbooks(...elts) {
  return terminal(...elts)._subType_("az_Workbooks");
}

/**
 * Create a az_Workflow dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Workflow(...elts) {
  return terminal(...elts)._subType_("az_Workflow");
}

/**
 * Create a az_Backlog dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Backlog(...elts) {
  return terminal(...elts)._subType_("az_Backlog");
}

/**
 * Create a az_Media dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Media(...elts) {
  return terminal(...elts)._subType_("az_Media");
}

/**
 * Create a az_Module dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Module(...elts) {
  return terminal(...elts)._subType_("az_Module");
}

/**
 * Create a az_Search_Grid dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Search_Grid(...elts) {
  return terminal(...elts)._subType_("az_Search_Grid");
}

/**
 * Create a az_Application_Insights dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Application_Insights(...elts) {
  return terminal(...elts)._subType_("az_Application_Insights");
}

/**
 * Create a az_Azure_DevOps dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_DevOps(...elts) {
  return terminal(...elts)._subType_("az_Azure_DevOps");
}

/**
 * Create a az_DevTest_Labs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_DevTest_Labs(...elts) {
  return terminal(...elts)._subType_("az_DevTest_Labs");
}

/**
 * Create a az_Lab_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Lab_Services(...elts) {
  return terminal(...elts)._subType_("az_Lab_Services");
}

/**
 * Create a az_SQL_Data_Warehouses dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Data_Warehouses(...elts) {
  return terminal(...elts)._subType_("az_SQL_Data_Warehouses");
}

/**
 * Create a az_Data_Factory dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Factory(...elts) {
  return terminal(...elts)._subType_("az_Data_Factory");
}

/**
 * Create a az_Azure_Synapse_Analytics dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Synapse_Analytics(...elts) {
  return terminal(...elts)._subType_("az_Azure_Synapse_Analytics");
}

/**
 * Create a az_Azure_Cosmos_DB dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Cosmos_DB(...elts) {
  return terminal(...elts)._subType_("az_Azure_Cosmos_DB");
}

/**
 * Create a az_Azure_Database_MySQL_Server dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Database_MySQL_Server(...elts) {
  return terminal(...elts)._subType_("az_Azure_Database_MySQL_Server");
}

/**
 * Create a az_Azure_Database_MariaDB_Server dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Database_MariaDB_Server(...elts) {
  return terminal(...elts)._subType_("az_Azure_Database_MariaDB_Server");
}

/**
 * Create a az_Azure_SQL_VM dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_SQL_VM(...elts) {
  return terminal(...elts)._subType_("az_Azure_SQL_VM");
}

/**
 * Create a az_Data_Factory dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Factory(...elts) {
  return terminal(...elts)._subType_("az_Data_Factory");
}

/**
 * Create a az_Virtual_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Clusters");
}

/**
 * Create a az_Elastic_Job_Agents dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Elastic_Job_Agents(...elts) {
  return terminal(...elts)._subType_("az_Elastic_Job_Agents");
}

/**
 * Create a az_SQL_Database dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Database(...elts) {
  return terminal(...elts)._subType_("az_SQL_Database");
}

/**
 * Create a az_Azure_Database_PostgreSQL_Server dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Database_PostgreSQL_Server(...elts) {
  return terminal(...elts)._subType_("az_Azure_Database_PostgreSQL_Server");
}

/**
 * Create a az_SQL_Server dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Server(...elts) {
  return terminal(...elts)._subType_("az_SQL_Server");
}

/**
 * Create a az_SQL_Elastic_Pools dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Elastic_Pools(...elts) {
  return terminal(...elts)._subType_("az_SQL_Elastic_Pools");
}

/**
 * Create a az_Managed_Database dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Managed_Database(...elts) {
  return terminal(...elts)._subType_("az_Managed_Database");
}

/**
 * Create a az_SQL_Managed_Instance dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_SQL_Managed_Instance(...elts) {
  return terminal(...elts)._subType_("az_SQL_Managed_Instance");
}

/**
 * Create a az_Azure_SQL_Server_Stretch_Databases dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_SQL_Server_Stretch_Databases(...elts) {
  return terminal(...elts)._subType_("az_Azure_SQL_Server_Stretch_Databases");
}

/**
 * Create a az_Cache_Redis dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cache_Redis(...elts) {
  return terminal(...elts)._subType_("az_Cache_Redis");
}

/**
 * Create a az_Instance_Pools dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Instance_Pools(...elts) {
  return terminal(...elts)._subType_("az_Instance_Pools");
}

/**
 * Create a az_Azure_Data_Explorer_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Data_Explorer_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Azure_Data_Explorer_Clusters");
}

/**
 * Create a az_Kubernetes_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Kubernetes_Services(...elts) {
  return terminal(...elts)._subType_("az_Kubernetes_Services");
}

/**
 * Create a az_Batch_Accounts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Batch_Accounts(...elts) {
  return terminal(...elts)._subType_("az_Batch_Accounts");
}

/**
 * Create a az_App_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Services(...elts) {
  return terminal(...elts)._subType_("az_App_Services");
}

/**
 * Create a az_Service_Fabric_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Fabric_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Service_Fabric_Clusters");
}

/**
 * Create a az_Container_Registries dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Container_Registries(...elts) {
  return terminal(...elts)._subType_("az_Container_Registries");
}

/**
 * Create a az_Workspaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Workspaces(...elts) {
  return terminal(...elts)._subType_("az_Workspaces");
}

/**
 * Create a az_Virtual_Machine dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Machine(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Machine");
}

/**
 * Create a az_Kubernetes_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Kubernetes_Services(...elts) {
  return terminal(...elts)._subType_("az_Kubernetes_Services");
}

/**
 * Create a az_Mesh_Applications dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Mesh_Applications(...elts) {
  return terminal(...elts)._subType_("az_Mesh_Applications");
}

/**
 * Create a az_Availability_Sets dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Availability_Sets(...elts) {
  return terminal(...elts)._subType_("az_Availability_Sets");
}

/**
 * Create a az_Disks_Snapshots dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Disks_Snapshots(...elts) {
  return terminal(...elts)._subType_("az_Disks_Snapshots");
}

/**
 * Create a az_OS_Images__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_OS_Images__Classic_(...elts) {
  return terminal(...elts)._subType_("az_OS_Images__Classic_");
}

/**
 * Create a az_Virtual_Machines__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Virtual_Machines__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Virtual_Machines__Classic_");
}

/**
 * Create a az_Function_Apps dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Function_Apps(...elts) {
  return terminal(...elts)._subType_("az_Function_Apps");
}

/**
 * Create a az_Cloud_Services__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cloud_Services__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Cloud_Services__Classic_");
}

/**
 * Create a az_Batch_Accounts dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Batch_Accounts(...elts) {
  return terminal(...elts)._subType_("az_Batch_Accounts");
}

/**
 * Create a az_Disks dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Disks(...elts) {
  return terminal(...elts)._subType_("az_Disks");
}

/**
 * Create a az_Images dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Images(...elts) {
  return terminal(...elts)._subType_("az_Images");
}

/**
 * Create a az_VM_Scale_Sets dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_VM_Scale_Sets(...elts) {
  return terminal(...elts)._subType_("az_VM_Scale_Sets");
}

/**
 * Create a az_App_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Services(...elts) {
  return terminal(...elts)._subType_("az_App_Services");
}

/**
 * Create a az_Service_Fabric_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Service_Fabric_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Service_Fabric_Clusters");
}

/**
 * Create a az_Image_Definitions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Image_Definitions(...elts) {
  return terminal(...elts)._subType_("az_Image_Definitions");
}

/**
 * Create a az_Image_Versions dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Image_Versions(...elts) {
  return terminal(...elts)._subType_("az_Image_Versions");
}

/**
 * Create a az_Shared_Image_Galleries dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Shared_Image_Galleries(...elts) {
  return terminal(...elts)._subType_("az_Shared_Image_Galleries");
}

/**
 * Create a az_VM_Images__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_VM_Images__Classic_(...elts) {
  return terminal(...elts)._subType_("az_VM_Images__Classic_");
}

/**
 * Create a az_Disks__Classic_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Disks__Classic_(...elts) {
  return terminal(...elts)._subType_("az_Disks__Classic_");
}

/**
 * Create a az_Container_Services__Deprecated_ dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Container_Services__Deprecated_(...elts) {
  return terminal(...elts)._subType_("az_Container_Services__Deprecated_");
}

/**
 * Create a az_Azure_Blockchain_Service dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Blockchain_Service(...elts) {
  return terminal(...elts)._subType_("az_Azure_Blockchain_Service");
}

/**
 * Create a az_Azure_Blockchain_Service dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Blockchain_Service(...elts) {
  return terminal(...elts)._subType_("az_Azure_Blockchain_Service");
}

/**
 * Create a az_AVS dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_AVS(...elts) {
  return terminal(...elts)._subType_("az_AVS");
}

/**
 * Create a az_Multi_Tenancy dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Multi_Tenancy(...elts) {
  return terminal(...elts)._subType_("az_Multi_Tenancy");
}

/**
 * Create a az_App_Service_Plans dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Service_Plans(...elts) {
  return terminal(...elts)._subType_("az_App_Service_Plans");
}

/**
 * Create a az_App_Service_Environments dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Service_Environments(...elts) {
  return terminal(...elts)._subType_("az_App_Service_Environments");
}

/**
 * Create a az_App_Service_Certificates dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Service_Certificates(...elts) {
  return terminal(...elts)._subType_("az_App_Service_Certificates");
}

/**
 * Create a az_App_Service_Domains dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Service_Domains(...elts) {
  return terminal(...elts)._subType_("az_App_Service_Domains");
}

/**
 * Create a az_Notification_Hub_Namespaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hub_Namespaces(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hub_Namespaces");
}

/**
 * Create a az_Search_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Search_Services(...elts) {
  return terminal(...elts)._subType_("az_Search_Services");
}

/**
 * Create a az_CDN_Profiles dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_CDN_Profiles(...elts) {
  return terminal(...elts)._subType_("az_CDN_Profiles");
}

/**
 * Create a az_Notification_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hubs");
}

/**
 * Create a az_App_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Services(...elts) {
  return terminal(...elts)._subType_("az_App_Services");
}

/**
 * Create a az_API_Management_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_API_Management_Services(...elts) {
  return terminal(...elts)._subType_("az_API_Management_Services");
}

/**
 * Create a az_Search_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Search_Services(...elts) {
  return terminal(...elts)._subType_("az_Search_Services");
}

/**
 * Create a az_Notification_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Notification_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Notification_Hubs");
}

/**
 * Create a az_App_Service_Environments dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_App_Service_Environments(...elts) {
  return terminal(...elts)._subType_("az_App_Service_Environments");
}

/**
 * Create a az_Log_Analytics_Workspaces dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Log_Analytics_Workspaces(...elts) {
  return terminal(...elts)._subType_("az_Log_Analytics_Workspaces");
}

/**
 * Create a az_HD_Insight_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_HD_Insight_Clusters(...elts) {
  return terminal(...elts)._subType_("az_HD_Insight_Clusters");
}

/**
 * Create a az_Event_Hubs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Hubs(...elts) {
  return terminal(...elts)._subType_("az_Event_Hubs");
}

/**
 * Create a az_Azure_Data_Explorer_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Data_Explorer_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Azure_Data_Explorer_Clusters");
}

/**
 * Create a az_Data_Factory dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Factory(...elts) {
  return terminal(...elts)._subType_("az_Data_Factory");
}

/**
 * Create a az_Stream_Analytics_Jobs dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Stream_Analytics_Jobs(...elts) {
  return terminal(...elts)._subType_("az_Stream_Analytics_Jobs");
}

/**
 * Create a az_Analysis_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Analysis_Services(...elts) {
  return terminal(...elts)._subType_("az_Analysis_Services");
}

/**
 * Create a az_Event_Hub_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Hub_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Event_Hub_Clusters");
}

/**
 * Create a az_Data_Lake_Store_Gen1 dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Lake_Store_Gen1(...elts) {
  return terminal(...elts)._subType_("az_Data_Lake_Store_Gen1");
}

/**
 * Create a az_Azure_Synapse_Analytics dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Azure_Synapse_Analytics(...elts) {
  return terminal(...elts)._subType_("az_Azure_Synapse_Analytics");
}

/**
 * Create a az_HD_Insight_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_HD_Insight_Clusters(...elts) {
  return terminal(...elts)._subType_("az_HD_Insight_Clusters");
}

/**
 * Create a az_Analysis_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Analysis_Services(...elts) {
  return terminal(...elts)._subType_("az_Analysis_Services");
}

/**
 * Create a az_Event_Hub_Clusters dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Event_Hub_Clusters(...elts) {
  return terminal(...elts)._subType_("az_Event_Hub_Clusters");
}

/**
 * Create a az_Data_Lake_Store_Gen1 dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Data_Lake_Store_Gen1(...elts) {
  return terminal(...elts)._subType_("az_Data_Lake_Store_Gen1");
}

/**
 * Create a az_Cognitive_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cognitive_Services(...elts) {
  return terminal(...elts)._subType_("az_Cognitive_Services");
}

/**
 * Create a az_Cognitive_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Cognitive_Services(...elts) {
  return terminal(...elts)._subType_("az_Cognitive_Services");
}

/**
 * Create a az_Bot_Services dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
export function az_Bot_Services(...elts) {
  return terminal(...elts)._subType_("az_Bot_Services");
}

