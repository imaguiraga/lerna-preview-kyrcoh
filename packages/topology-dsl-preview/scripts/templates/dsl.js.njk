const {
  fanIn,
  fanOut,
  fanOut_fanIn,
  group,
  sequence,
  optional,
  repeat,
  choice,
  merge,
  branch,
  terminal
} = require("@imaguiraga/topology-dsl-core");

{% for item in items %}
/**
 * Create a '{{ item.dsl }}' dsl tree.
 * @param {array|object} elts - The elements.
 * @return {object} dsl object.
 */
function {{ item.dsl }}(...elts) {
  return {{ item.tagName }}(...elts)._subType_("{{ item.subType }}");
}
module.exports.{{ item.dsl }} = {{ item.dsl }};

{% endfor %}
